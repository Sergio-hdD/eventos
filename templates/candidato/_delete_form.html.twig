<form method="post" action="{{ path('app_candidato_delete', {'id': candidato.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ candidato.id) }}">
    <button class="btn">Delete</button>
</form>
